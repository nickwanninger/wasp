#pragma once



void putchar(char);
int puts(char*);
int printk(const char* format, ...);
int sprintk(char* buffer, const char* format, ...);
int snprintk(char* buffer, int count, const char* format, ...);
